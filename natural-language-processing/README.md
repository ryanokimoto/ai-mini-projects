# This project contains 3 python files that run independently 

## n-gram.py and smoothing.py can be run simply with

 `python3 (n-gram.py, smoothing.py)`

## when running opt-experiment.py first run

`pip install requirements.txt`
then you can run

`python3 opt-experiment.py`